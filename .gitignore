# dependencies
node_modules
.pnpm-store

# WXT / build outputs
.wxt
.output
dist
web-ext-artifacts
*.zip

# generated icons (regenerated on build via scripts/generate-icons.mjs)
public/icon/*.png

# secrets & local env
.env
.env.*
!.env.example

# editors / OS
.vscode
.idea
.DS_Store
Thumbs.db

# logs / coverage / typescript build info
*.log
coverage
*.tsbuildinfo

# private notes / scratch
.afn
.planning
.claude
notes.local.md
TODO.local.md

# private design notes (kept local, not published)
docs/

# store assets are regenerated via scripts/generate-store-assets.mjs;
# only the README hero GIF is tracked (it's the demo preview).
store-assets/*.png
store-assets/screenshots/
!store-assets/scrollsaber-demo.gif
